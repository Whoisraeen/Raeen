[package]
name = "raeen-hle"
description = "High-Level Emulation of PS5 system libraries (libkernel, libSceGnmDriver, etc.)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
raeen-core = { workspace = true }
raeen-kernel = { workspace = true }
raeen-gpu = { workspace = true }
raeen-audio = { workspace = true }
raeen-input = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
dashmap = { workspace = true }
# Linear-time substring/byte search (Two-Way / SIMD) for hle_strstr — avoids
# the O(n·m) DoS a naive windows()-scan exposes to guest-controlled inputs.
memchr = "2"
# OS-backed CSPRNG for libSceRandom; never exposes host identifiers or a
# deterministic emulator-global seed to titles.
getrandom = "0.3"
sha1 = { workspace = true }
# Real JSON parsing for libSceJson2 Parser::parse (host-side value model).
serde_json = { workspace = true }

[dev-dependencies]
# The PM4 command processor, so an AGC packet-emitter test can walk its OWN
# emitted DWORDs the way the GPU will. Emitter and walker each looked correct in
# isolation while disagreeing on the DRAW_INDEX_2 body layout; only an
# end-to-end test catches that class of bug. Dev-only — no production coupling.
kyty-graphics = { workspace = true }

# GetTimeZoneInformation for libSceRtc's UTC→local conversion (Windows-only).
[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }
