[package]
name = "raeen-input"
description = "DualSense controller emulation with haptic feedback and adaptive triggers"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
raeen-core = { workspace = true }
gilrs = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }

# Native, mapping-DB-free readers (XInput + raw-HID DualSense). Windows-only;
# the modules are #[cfg(windows)]-gated so non-Windows targets still build.
[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }
